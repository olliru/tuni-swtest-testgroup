import 'mocha';

import * as assert from 'assert';

import add from '../src/capitalize.js';


describe('Util library', function() {
  describe('capitalize', function() {
    // it('should return upper letters to first letter upper and other lower', function() {
    //   assert.equal(capitalize('MILK'), 'Milk');
    // });
  });
});